{
  "name": "practice-app",
  "version": "1.0.0",
  "description": "Calculator",
  "scripts": {
    "build": "parcel build src/index.html --public-url /practice-app-location",
    "start": "parcel src/index.html",
    "lint": "eslint src/*.js",
    "test": "jest",
    "deploy": "npm run build && gh-pages -d dist"
  }
  "devDependencies": {
    "eslint": "^8.35.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "gh-pages": "^5.0.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "parcel-bundler": "^1.12.5",
  }
}
